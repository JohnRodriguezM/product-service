---
// Component Imports
import MainHead from "../components/MainHead.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import PortfolioPreview from "../components/PortfolioPreview.astro";
import type { Project } from "../types";

// Data Fetching: List all Markdown posts in the repo.
const projects = await Astro.glob<Project>("./project/**/*.md");
const featuredProject = projects[0]!;

// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/
---

<html lang="en">
  <head>
    <MainHead
      title="Las gorditas de la alborada sitio oficial"
      description="Las gorditas de la alborada sitio oficial"
    />
    <style>
      .wrapper {
        margin-top: 2em;
        margin-bottom: 2em;
      }

      .hero {
        position: relative;
        overflow: hidden;
      }
      .hero {
        position: relative;
        overflow: hidden;
      }
      @media (min-width: 750px) {
        .hero {
          height: 45vw;
        }
      }
      @media (min-width: 750px) {
        .hero {
          height: 45vw;
        }
      }
      .img {
        display: block;
        width: 100%;
        height: auto;
      }
      .gradient,
      .gradient2 {
        opacity: 0.3;
        background-image: url("https://www.recetasderechupete.com/wp-content/uploads/2019/11/Tipos-de-arepa-2.jpg");
        background-size: cover;
        pointer-events: none;
        mix-blend-mode: screen;
        width: 100%;
        height: 100%;
        position: absolute;
        top: 0;
        left: 0;
        z-index: 2;
      }
      .gradient2 {
        mix-blend-mode: multiply;
        background-size: cover;
      }
      .overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 10;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: center;
        padding-left: 2rem;
      }
      @media (min-width: 750px) {
        .overlay {
          padding-left: 4rem;
        }
      }
      .title {
        font-weight: 900;
        font-size: var(--f-u4);
        margin-bottom: 0.5rem;
        margin-top: 0;
      }
      @media (min-width: 750px) {
        .title {
          font-size: var(--f-u12);
        }
      }
      .grid {
        display: grid;
        grid-gap: 2rem;
      }
      @media (min-width: 1200px) {
        .grid {
          grid-template-columns: 2fr 1fr;
        }
      }
      .sectionTitle {
				text-align: center;
        font-weight: 700;
        font-size: var(--f-u6);
        margin-top: 4rem;
        margin-bottom: 2rem;
      }
      .roles {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5em;
        font-size: var(--f-d1);
      }
      .role {
        position: relative;
        display: inline-block;
        white-space: nowrap;
        font-weight: 900;
        color: var(--t-bg);
        background-color: var(--t-fg);
        padding: 0.25em 0.5em;
        z-index: 2;
      }
      @media (min-width: 750px) {
        .role {
          font-size: var(--f-u3);
        }
      }
      .role:nth-of-type(1) .invert {
        background-color: var(--c-pink);
      }

      .role:nth-of-type(2) .invert {
        background-color: var(--c-blue);
      }

      .role:nth-of-type(3) .invert {
        background-color: var(--c-green);
      }

      .role:hover .invert {
        clip-path: polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%);
      }
      .invert {
        position: absolute;
        color: var(--t-fg);
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
        pointer-events: none;
        clip-path: polygon(0% 100%, 100% 100%, 100% 200%, 0% 200%);
        transition: clip-path cubic-bezier(0.4, 0, 0.5, 1) 150ms;
      }
      .desc {
        font-weight: bold;
        font-size: var(--f-u4);
        margin-top: 1.5rem;
        margin-bottom: 0;
      }
      .subtitle {
        display: block;
        font-weight: 400;
        font-size: var(--f-d6);
        letter-spacing: -0.0625em;
      }
      .bio {
        text-align: center;
        line-height: 2;
        margin-bottom: 2rem;
      }
      .bio > span:first-of-type {
        text-align: center;
        line-height: 1;
        margin-bottom: 0.5em;
        display: block;
        font-weight: 700;
        font-size: var(--f-u4);
      }
      .buttonContainer {
        text-align: center;
        margin-top: 2em;
      }
			.buttonContainer:hover{
				transform: scale(1.1);
			}
			.buttonContainer:active{
				transform: scale(.9);
			}
			.buttonContainer > a {
				margin: 0 0.5em;
				list-style: none;
				text-decoration: none;
			}
    </style>
  </head>
  <body>
    <Nav />
    <header class="hero">
      <img width="1600" height="1131" class="img" />
      <div class="gradient"></div>
      <div class="gradient2"></div>
      <div class="overlay">
        <h2 class="title">Las Gorditas de la Alborada</h2>
        <div class="roles">
          <span class="role"
            >🫓 Las mejores arepas del mundo <span class="invert"
              >🫓 Disfrútalas</span
            >
          </span>
        </div>
        <marquee
          behavior="scroll"
          direction="left"
          scrollamount="10"
          scrolldelay="12"
          width="85%"
          class="desc">No puedes comprar la felicidad, pero puedes comprar arepa, que es casi lo mismo.</marquee
        >
      </div>
    </header>
    <main class="wrapper">
      <div class="grid">
        <div class="section">
          <h2 class="sectionTitle">Nuestro centro</h2>
          <PortfolioPreview project={featuredProject} />
          <div class="buttonContainer">
            <a href="/productos/" class="button">Ver los productos</a>
          </div>
          <div class="buttonContainer">
            <a href="/opinion/" class="button">Danós tu opinión</a>
          </div>
        </div>
        <div class="section">
          <h3 class="sectionTitle">Sobre nosotros</h3>
          <p class="bio">
            <span>Hola!</span>
            <b>S</b>omos un emprendimiento llanero que fabrica arepas al por
            mayor y al detal. Nuestro
            centro de atención está ubicado en la ciudad de Villavicencio,
            Colombia.
            <a
              href="https://github.com/withastro/astro"
              target="_blank"
              rel="nofollow"></a>
            <br />
            <b>N</b>acimos en el año 2018, con la idea de ofrecer a nuestros
            clientes un producto de calidad y con un sabor único.
            <br />
            <b> N</b>uestro objetivo es que nuestros clientes disfruten de las
            mejores arepas del mundo, asi que esperamos tener el placer de atenderlos pronto
          </p>
          <!--<p>
            <a href="/about">Read more</a>
          </p>-->
        </div>
      </div>
    </main>
    <Footer />
  </body>
</html>
