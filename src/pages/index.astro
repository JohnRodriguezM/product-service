---
// Component Imports
import MainHead from "../components/MainHead.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import PortfolioPreview from "../components/PortfolioPreview.astro";
import type { Project } from "../types";

// Data Fetching: List all Markdown posts in the repo.
const projects = await Astro.glob<Project>("./project/**/*.md");
const featuredProject = projects[0]!;

// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/
---

<html lang="en">
  <head>
    <MainHead
      title="Miss Arepas | Inicio "
      description="Miss arepas sitio oficial"
    />
    <style>
      .wrapper {
        margin: 0 auto;
        margin-top: 2em;
        margin-bottom: 2em;
      }

      .hero {
        display: flex;
        padding-top: 3em;
        position: relative;
        overflow: hidden;
      }
      .hero {
        position: relative;
        overflow: hidden;
      }
      @media (min-width: 750px) {
        .hero {
          height: 45vw;
        }
      }
      @media (min-width: 750px) {
        .hero {
          height: 45vw;
        }
      }
      .img {
        display: block;
        width: 100%;
        height: auto;
      }
      .gradient,
      .gradient2 {
        opacity: 0.3;
        background-image: url("https://media.gq.com.mx/photos/5c054d335c1fcb20624c5596/16:9/w_2560%2Cc_limit/GettyImages-1006838344.jpg");
        background-size: cover;
        pointer-events: none;
        mix-blend-mode: screen;
        width: 100%;
        height: 100%;
        position: absolute;
        top: 0;
        left: 0;
        z-index: 2;
      }
      .gradient2 {
        mix-blend-mode: multiply;
        background-size: cover;
      }
      .overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 10;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: center;
        padding-left: 2rem;
      }
      @media (min-width: 750px) {
        .overlay {
          padding-left: 4rem;
        }
      }
      .title {
        width: 70%;
        font-weight: 900;
        font-size: var(--f-u4);
        margin-bottom: 0.5rem;
        margin-top: 50px;
      }
      @media (min-width: 750px) {
        .title {
          font-size: var(--f-u12);
        }
      }
      .grid {
        display: grid;
        grid-gap: 2rem;
      }
      @media (min-width: 1200px) {
        .grid {
          grid-template-columns: 2fr 1fr;
        }
      }
      .sectionTitle {
        text-align: center;
        font-weight: 700;
        font-size: var(--f-u4);
        margin-top: 2.8rem;
        margin-bottom: 2rem;
      }
      .roles {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5em;
        font-size: var(--f-d1);
      }
      .role {
        position: relative;
        display: inline-block;
        white-space: nowrap;
        font-weight: 900;
        color: var(--t-bg);
        background-color: var(--t-fg);
        padding: 0.25em 0.5em;
        z-index: 2;
      }
      .role:hover {
        cursor: default;
      }
      @media (min-width: 750px) {
        .role {
          font-size: var(--f-u3);
        }
      }
      .role:nth-of-type(1) .invert {
        background-color: var(--c-blue);
      }
      .role:hover .invert {
        clip-path: polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%);
      }
      .invert {
        position: absolute;
        color: var(--t-fg);
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
        pointer-events: none;
        clip-path: polygon(0% 100%, 100% 100%, 100% 200%, 0% 200%);
        transition: clip-path cubic-bezier(0.4, 0, 0.5, 1) 150ms;
      }
      .desc {
        font-weight: bold;
        font-size: var(--f-u1);
        margin-top: 1.5rem;
        margin-bottom: 0;
      }
      .subtitle {
        display: block;
        font-weight: 400;
        font-size: var(--f-d6);
        letter-spacing: -0.0625em;
      }
      .bio {
        text-align: center;
        line-height: 2;
        margin-bottom: 2rem;
      }
      .bio > span:first-of-type {
        text-align: center;
        line-height: 1;
        margin-bottom: 0.5em;
        display: block;
        font-weight: 700;
        font-size: var(--f-u4);
      }
      .buttonContainer {
        text-align: center;
        margin-top: 2em;
      }
      .buttonContainer:hover {
        transform: scale(1.1);
      }
      .buttonContainer:active {
        transform: scale(0.9);
      }
      .buttonContainer > a {
        margin: 0 0.5em;
        list-style: none;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <Nav />
    <header class="hero">
      <img width="1600" height="1131" class="img" />
      <div class="gradient"></div>
      <div class="gradient2"></div>
      <div class="overlay">
        <h2 class="title">Miss Arepas</h2>
        <div class="roles">
          <span class="role">🫓 Las mejores del mundo… ¡y mucho más! 👨‍🍳</span>
        </div>
        <marquee
          behavior="scroll"
          direction="left"
          scrollamount="10"
          scrolldelay="15"
          width="90%"
          class="desc"
          >No puedes comprar la felicidad, pero puedes comer en Miss arepas, que
          es casi lo mismo.</marquee
        >
      </div>
    </header>
    <main class="wrapper">
      <div class="grid">
        <div class="section">
          <h2 class="sectionTitle">Lo que ofrecemos 👨‍🍳</h2>
          <PortfolioPreview project={featuredProject} />
          <div class="buttonContainer">
            <a href="/productos/" class="button">Explora nuestros productos</a>
          </div>
          <div class="buttonContainer">
            <a href="/opinion/" class="button">¡Queremos saber tu opinión!</a>
          </div>
        </div>
        <div class="section">
          <h3 class="sectionTitle">Acerca de nosotros</h3>
          <p class="bio">
            <span style="margin-bottom: 2rem"
              >¡Pasión en cada arepa y mucho más!</span
            >
            <b>¡S</b>omos un emprendimiento llanero que no solo ofrece las
            mejores arepas al por mayor y al detal, sino que también queremos
            llevarte una amplia variedad de comida y sabores auténticos.
            Ubicados en Villavicencio, Colombia, desde 2018 trabajamos para que
            cada bocado sea una experiencia únic<b>a!</b>
            <br />
            <br />
            <b>¿L</b>isto para descubrir lo mejor de la gastronomía colombiana?
            ¡Visítanos y explora nuestra creciente oferta de sabores delicioso<b
              >s!</b
            >
            <br />
            <br />
            <b>¡V</b>isítanos y déjanos consentirte con lo mejor de la
            gastronomía colombian<b>a!</b>
          </p>
        </div>
      </div>
    </main>
    <Footer />
  </body>
</html>
